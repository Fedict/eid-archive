#!/usr/bin/make -f

%: 
	dh $@

override_dh_auto_build:

override_dh_install: 69fa2d05.gpg
	dh_install

override_dh_auto_clean:
	rm -f 69fa2d05.gpg
	dh_auto_clean

override_dh_builddeb:
	dh_builddeb -- --no-uniform-compression

%.gpg: %.asc
	gpg --dearmor < $< > $@
